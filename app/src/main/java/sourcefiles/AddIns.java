/**
 * Creates a enum
 * The AddIns enum class has a unique order number and keeps the list of menu items added by the user
 * @author Kevin Liu, Gurveer Aulakh
 */
package sourcefiles;

public enum AddIns {
    CREAM,
    SYRUP,
    MILK,
    CARAMEL,
    WHIPPED_CREAM;
}